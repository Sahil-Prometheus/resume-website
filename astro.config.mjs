import { defineConfig } from 'astro/config';

export default defineConfig({
  // Placeholder domain — update when the real domain is chosen.
  // This is used for the canonical link and social share image URLs.
  site: 'https://sahilgohel.com',
  devToolbar: { enabled: false },
});
